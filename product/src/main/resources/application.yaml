spring:
  application:
    name: productos
  datasource:
    url: jdbc:mysql://localhost:3306/db_product?serverTimezone=UTC
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
  jpa:
    database-platform: org.hibernate.dialect.MySQLDialect
    hibernate:
      ddl-auto: update

cloud:
  openfeign:
    circuitbreaker:
      enabled: true

server:
  port: 8082

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka



# Puerto en el que va a correr este segundo microservicio (¡Tienen que ser distintos!)





